"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stepsFor = void 0;
const statuses_1 = require("./statuses");
function stepsFor(transfer, steps, descriptions) {
    const completed = steps.indexOf(String(transfer.completedStep));
    return steps.map((key, i) => ({
        key,
        description: descriptions[key],
        status: transfer.status === statuses_1.FAILED && i === completed + 1
            ? statuses_1.FAILED
            : i <= completed ? 'completed' : 'pending'
    }));
}
exports.stepsFor = stepsFor;
//# sourceMappingURL=i18nHelpers.js.map